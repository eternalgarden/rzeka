namespace Looming
{
    public static class RzekaOperators
    {
        //public static IObservable<T> Weave<T,Q,A>(
        //    this IObservable<T> source,
        //    Func<T, IObservable<Q>, IObservable<A>> spell,
        //    LoomingRzeka rzeka = default)
        //        where Q : IMatter
        //{
        //    return Observable.Create<Q>(subscribe: observer =>
        //    {
        //        Type keyT = typeof(Dream<Q>);
        //        var oT = _web[keyT] as Dream<T>;


        //        return source.Subscribe(
        //            onNext: t =>
        //            {
        //                spell.Invoke(t);
        //            });

        //    });
        //}
    }
}