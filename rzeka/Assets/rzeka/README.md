# Reactive Extensions

- https://github.com/dotnet/reactive
- so! some project release was actually pulled here and butchered out of the unsupported companion components of Reactive Extensions: 
  - `AsyncRx.NET` bye!
  - `Ix.NET` bye!
  - that leaves us with just `Rx.NET`
  - it would be though neat to know which version we used, so on next update, preferably when unity supports net8, note that down!